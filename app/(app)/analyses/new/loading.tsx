import { DetailSkeleton } from "@/components/skeleton";

/** B: ontbrak nog. Wacht op de lijst met merken voordat het formulier vult. */
export default function Loading() {
  return <DetailSkeleton />;
}
