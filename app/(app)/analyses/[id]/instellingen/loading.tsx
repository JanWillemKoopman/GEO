import { ChapterSkeleton } from "@/components/skeleton";

/** B: ontbrak nog. Zelfde queries als het conceptscherm, nu op de beheerplek. */
export default function Loading() {
  return <ChapterSkeleton blocks={3} />;
}
