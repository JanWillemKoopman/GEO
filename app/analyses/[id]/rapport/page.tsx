import { EmptyState } from "@/components/empty-state";

export default function RapportPage() {
  return (
    <EmptyState title="Nog geen rapport">
      Zodra de meting klaar is, analyseren we waar concurrenten je voorbijstreven en krijg je
      hier concrete aanbevelingen — met een knop om de bijbehorende pagina te laten schrijven.
    </EmptyState>
  );
}
